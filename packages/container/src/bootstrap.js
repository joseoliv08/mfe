import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// The App component will be rendered in the html that has an element with the ID of 'root'
ReactDOM.render(
    <App />, document.querySelector('#root')
);